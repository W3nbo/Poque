# Poque — PRD

> 一款朋友消遣向的德州扑克小游戏。所有"代币"都叫 **积分（chip）**，每局赛事独立结算，无货币积累。支持局域网好友联机，以及 DeepSeek 驱动的 AI 单机对战。

---

## 0. 文档信息

| 字段 | 内容 |
| --- | --- |
| 产品名 | **Poque** |
| 版本 | v0.2 |
| 修订日期 | 2026-09-11 |
| 文档状态 | 待评审 |
| 目标读者 | 产品 / 设计 / 研发 |

---

## 1. 产品概述

### 1.1 一句话定义
Poque 是一款**局域网 2–9 人热座联机 + DeepSeek AI 单机对战**的德州扑克小游戏，参考 Codex 德州桌面应用的极简视觉风格，强调"开局快、玩完即散、零学习成本"。

### 1.2 设计原则
1. **零摩擦开局**：从点开网页到第一手牌 ≤ 60 秒
2. **一局一清**：赛事独立，无存档货币，无成长体系，无留存压力
3. **桌面优先**：1440px 以上桌面浏览器，不做手机适配
4. **极简交互**：牌桌周围只显示必要信息，操作条只有 3 个按钮
5. **朋友氛围**：可换主题，但不堆社交功能（无聊天、无表情、无音效）
6. **AI 单机可玩**：一个人也能开局，DeepSeek 驱动 AI + 规则兜底，永不卡局

### 1.3 不做什么（Out of Scope）
- ❌ 真实代币 / 现金 / 充值
- ❌ 用户账号系统 / 登录
- ❌ 历史赛事存档 / 战绩复盘
- ❌ 联机模式中的 AI 补位（AI 仅存在于单机模式）
- ❌ 公网匹配 / 陌生人对战
- ❌ 手机 / 平板适配
- ❌ 牌桌内聊天 / 表情 / 音效
- ❌ 商业化与留存机制

---

## 2. 目标用户

### 2.1 主要场景
- **朋友线下聚会**：几个人围在一台电脑前热座轮流，或各自用电脑在同一 WiFi 下联机
- **远程消遣**：异地好友通过 VPN 或同局域网（公司 / 宿舍）联机
- **单人消遣**：一个人也能开完整赛事，对手为 DeepSeek 驱动的 AI 玩家

### 2.2 用户特征
- 熟悉德州扑克基本规则，不需要教学
- 不追求竞技深度，享受消遣
- 容忍网络小延迟，但反感复杂配置

---

## 3. 核心特性总览

| 模块 | 描述 |
| --- | --- |
| **赛事创建** | 设置赛事名、房间码、初始积分、玩家上限、起始盲注级别 |
| **赛事加入** | 输入 4 位房间码 + 昵称 + emoji 头像即可入座 |
| **mDNS 自动发现** | 同局域网打开页面即可看到房间列表 |
| **标准 NLHE 对局** | 标准无限注德州，MTT 式盲注递增表 |
| **限时操作** | 15 秒未操作自动 Check/Fold |
| **断线保留** | 玩家掉线保留席位，重连期间自动 Check/Fold |
| **积分结算** | 淘汰后只剩 1 人时结束，柱状图展示净胜积分 |
| **主题切换** | 单色背景 + 亮色牌桌，可换预设主题 |
| **AI 单机模式** | DeepSeek 驱动的 1–8 个 AI 对手，LLM + 规则兜底，复用完整赛事流程 |

---

## 4. 详细功能需求

### 4.1 首页（Home）

#### 4.1.1 布局
- 左侧固定侧边栏（260px）：导航 + 主题切换
- 中央主区域：当前局域网可加入的房间列表 + 行动按钮
- 顶部状态栏：当前客户端版本、连接状态指示灯

#### 4.1.2 区域内容

**侧边栏（参考 Codex）**
- Logo + 产品名 "Poque"
- 导航项：
  - 🏠 Home（房间列表）
  - 📜 Recent（最近玩过的赛事，仅本地缓存）
  - ⚙️ Settings（DeepSeek API Key 配置 + 通用偏好）
- 底部：主题切换下拉菜单（Light / Dark）

**主区域**
- **创建赛事按钮**（主按钮，宽 320px，居中偏上）
- **单机 vs AI 按钮**（次主按钮，与创建赛事并列；未配置 DeepSeek API Key 时置灰，点击提示先到设置页配置）
- **加入赛事输入区**：
  - 4 位数字房间码输入框（自动跳格）
  - "加入" 按钮（输入 4 位后激活）
- **局域网可见房间列表**（mDNS 发现，自动刷新）：
  - 卡片样式：赛事名 + 房主昵称 + 当前人数 / 上限 + 起始盲注级别
  - 鼠标悬停高亮，点击直接加入

#### 4.1.3 交互细节
- 房间码输入校验：纯数字，0–9，4 位
- 加入前必须填写昵称（2–12 字符，支持中文/英文/emoji）
- emoji 头像从固定池选择（30 个预设：🐶🐱🦊🐼🐯🐸🐵🐔🐧🦄🐝🐢🦖🐳🐬🦈🐙🦀🌵🌹🌻🍀⭐️🎈🎲🎮🎯🎨🎭🎪）

#### 4.1.4 设置页（Settings）
- **DeepSeek API 配置**
  - API Key 输入框（密码态显示，存 localStorage，仅本机）
  - 模型选择：`deepseek-chat`（默认）/ `deepseek-reasoner`
  - 「测试连接」按钮：发送一条最小请求验证 Key 可用性，显示延迟与结果
- **通用偏好**
  - 主题切换
  - 默认昵称 / emoji 记忆（下次进房预填）

---

### 4.2 赛事配置页（Create Tournament）

仅建赛者（Host）可见。布局采用单页表单，分 3 个步骤（不分页，仅分组卡片）。

#### 4.2.1 基本信息
| 字段 | 类型 | 默认值 | 范围 |
| --- | --- | --- | --- |
| 赛事名称 | 文本输入 | "Poque 局" | 1–20 字符 |
| 房间码 | 自动生成（可点击 🔄 重生成） | 1000–9999 随机 | 4 位数字 |
| 玩家上限 | 数字选择器 | 6 | 2–9 |

> 单机 vs AI 模式复用本表单，但隐藏房间码字段，新增「AI 数量」字段（1–8，默认 3）。

#### 4.2.2 积分设置
| 字段 | 类型 | 默认值 | 范围 |
| --- | --- | --- | --- |
| 每人初始积分 | 数字输入 | 1000 | 100–100000 |
| 起始盲注级别 | 下拉选择 | Level 1 (10/20) | 1–10 |
| 盲注递增节奏 | 单选 | 标准 MTT | 详见 §4.6 |

#### 4.2.3 确认
- "开始赛事" 按钮（高亮主按钮）
- 创建后页面自动跳转到赛事大厅（联机）或直接开局（单机 AI）

---

### 4.3 赛事大厅（Tournament Lobby）

#### 4.3.1 布局
- 顶部状态条：赛事名 + 房间码（可复制）+ "离开" 按钮
- 中央：椭圆牌桌（与对局界面同款，但无牌）
- 牌桌周围：已加入玩家座位（按加入顺序）
- 右侧栏：玩家列表（实时刷新）

#### 4.3.2 玩家状态
每个座位显示：
- emoji 头像（80×80px）
- 昵称
- 状态徽章：「就绪 ✓」/ 「未就绪…」/ 「房主 👑」
- 未就绪时座位灰色半透明

#### 4.3.3 交互
- 玩家加入/离开自动刷新座位
- 房主可踢人（点击玩家座位 → 弹出菜单 → 踢出）
- 全部玩家就绪后，"开始赛事" 按钮亮起
- 房主可点击 "开始赛事" 提前开局（未满员也可）

---

### 4.4 对局界面（Game Table）

完全复刻 Codex 风格：椭圆牌桌 + 玩家头像环绕 + 中央公共牌 + 底部操作条。

#### 4.4.1 牌桌布局（1440px 设计基准）

```
┌──────────────────────────────────────────────────────────┐
│  [Tour name] [Hand #3]                [Leave] [Copy id]  │  ← 顶部状态条 (56px)
├──────────────────────────────────────────────────────────┤
│                                                          │
│   ◯ 玩家1                ◯ 玩家2                        │
│         玩家6        中央公共牌区      玩家3             │
│              [公共牌 1][2][3][4][5]                      │
│   ◯ 玩家5                ◯ 玩家4                        │
│                                                          │
│                       ◯ 当前玩家（自己）                  │
│                                                          │
├──────────────────────────────────────────────────────────┤
│  [我的手牌：♠2 ♥A]                                       │  ← 手牌区 (96px)
│  [25%][33%][75%][133%][ALL]  |  [Fold][Call 50][Bet 100]│  ← 操作条 (96px)
└──────────────────────────────────────────────────────────┘
```

#### 4.4.2 玩家头像信息卡（参考截图样式）
每个玩家位显示：
- 头像（48–64px 圆图）
- 昵称 + 「🤖 AI / 👑 房主 / 离线」徽章
- 积分余额（chip 数字）
- 当前下注额（本手已投入）
- 手牌（仅自己可见；摊牌时全员可见）

#### 4.4.3 中央公共牌区
- 5 个牌位，按顺序亮起（preflop 全部牌背）
- 牌面设计：白底 + 大号数字/字母 + 花色图标
- 牌背：选中的主题对应纹理

#### 4.4.4 操作条（核心交互区）

**比例快捷键**（参考 Codex 截图）：
- 25% / 33% / 75% / 133% / ALL：点击按当前底池的对应比例下注
- 滑块：在比例间微调
- 数字输入框：手动输入具体下注额

**核心按钮（参考 Codex）**：
- `Fold`（左侧次按钮）
- `Call XX`（中间主按钮，显示当前需跟注额）
- `Bet/Raise XX`（右侧高亮主按钮，显示默认加注额）
- 按钮颜色：Fold=灰，Call=白底黑字，Bet=黑色实心

**操作限制**：
- 限时 15 秒，倒计时环显示在操作条上方
- 超时未操作：若可 Check 则自动 Check，否则 Fold
- 仅当前玩家可操作，其他玩家操作条灰显
- 单机 AI 模式下，AI 行动期间自己的操作条灰显并显示「AI 思考中…」

#### 4.4.5 状态反馈
- 当前行动玩家头像加蓝色高亮边框
- 玩家行动时头像轻微缩放动画
- 弃牌玩家头像变灰并加 ✕ 标记
- 全押（All-in）头像加 🔥 标记
- 摊牌时所有玩家手牌翻开放大显示在各自座位下方

---

### 4.5 赛事结算页（Results）

#### 4.5.1 触发条件
- 淘汰赛制：只剩 1 名玩家有积分（其他人全部 0 chip）
- 房主可手动提前结束

#### 4.5.2 布局
- 全屏覆盖层，半透明背景模糊
- 中央卡片（800×600px）：
  - 顶部："🏆 赛事结束 — {赛事名}"
  - 中部：**排名柱状图**（核心展示）
  - 底部："再开一局" 按钮（房主可见）/ "返回首页"

#### 4.5.3 排名柱状图规范
- 横轴：玩家昵称 + emoji 头像
- 纵轴：净胜积分（chip，正向）
- 柱状图样式：
  - 柱顶显示具体数字（如 "+850" 或 "-300"）
  - 正向柱用主色，负向柱用警告色
  - 冠军柱顶部带 🏆 图标
  - 按净胜积分从高到低排序
- 右侧补充列：最终剩余 chip、手数（参与手数）、最大单手盈亏

---

### 4.6 盲注递增表（MTT 预设）

#### 4.6.1 标准表（默认）
| Level | Small Blind | Big Blind | Ante | 持续手数 |
| ----- | ----------- | --------- | ---- | -------- |
| 1     | 10          | 20        | 0    | 直到下一级 |
| 2     | 15          | 30        | 0    | |
| 3     | 25          | 50        | 0    | |
| 4     | 50          | 100       | 0    | |
| 5     | 75          | 150       | 0    | |
| 6     | 100         | 200       | 25   | |
| 7     | 150         | 300       | 25   | |
| 8     | 250         | 500       | 50   | |
| 9     | 400         | 800       | 50   | |
| 10    | 600         | 1200      | 100  | |

> 初始积分 1000 对应前 6 级安全期，10 级达到 1500+ 下注级别，进入白热化。

#### 4.6.2 节奏控制
- **手动模式**：每手结束后房主可点 "下一级别"
- **自动模式**（默认）：每手结束后 5 秒自动升级
- 房主可在设置中切换
- 单机 AI 模式默认自动升级，玩家（自己）可在对局中手动切换

---

### 4.7 断线与超时处理

| 场景 | 行为 |
| --- | --- |
| 玩家 15 秒未操作 | 可 Check → 自动 Check；否则自动 Fold |
| 玩家断网 | 保留席位 5 分钟，期间到该玩家行动自动 Check/Fold |
| 5 分钟未重连 | 视为弃赛，强制 All-in 摊牌或淘汰 |
| 房主断线 | 自动移交权限给下一位玩家；若全断则赛事暂停 |

> 单机 AI 模式无断线概念：DeepSeek API 异常时按 §6.4 降级规则处理，赛事永不中断。

---

### 4.8 主题系统

提供 2 套预设主题（参考 Codex 极简美学）：

| 主题 | 背景色 | 牌桌色 | 强调色 | 适用 |
| --- | --- | --- | --- | --- |
| Light | `#F5F5F7` | `#E8E5DC` | `#1A1A1A` | 白天 |
| Dark | `#1C1C1E` | `#2C2C2E` | `#0A84FF` | 夜晚 |

主题仅前端 CSS 变量切换，不影响游戏逻辑。

---

### 4.9 AI 单机模式（Solo vs AI）

#### 4.9.1 入口与配置
- 首页「单机 vs AI」按钮进入，配置页复用联机赛事表单（赛事名 / 每人初始积分 / 玩家上限），外加：
  - **AI 数量**：1–8 个（自己 + AI ≥ 2 人即可成局）
- **无房间码、无大厅等待**，点击「开始赛事」立即发牌
- 前置条件：设置页已配置可用的 DeepSeek API Key

#### 4.9.2 AI 玩家形象
- 自动从预设池分配昵称 + emoji 头像（不与真人重复）
- 预设池示例：🦊 Finn、🐼 Panda、🐙 Ink、🐳 Wade、🦖 Rex、🐝 Buzz、🐸 Hopper、🐧 Pip

#### 4.9.3 决策管线
```
轮到 AI 行动
  → 构造 prompt（规则说明 + 当前牌面状态 JSON + 本手行动历史）
  → 经本地 Node 代理调用 DeepSeek（deepseek-chat）
  → 期望返回 JSON { "action": "...", "amount": n }
  → 合法性校验（动作是否可用、金额是否在筹码范围内）
  ├── 合法     → 执行该动作
  └── 非法/解析失败/超时 8s → 规则引擎兜底决策
```

#### 4.9.4 兜底规则引擎
- Chen 公式起手牌强度 + 底池赔率 + 位置修正
- 简单三段式：强牌加注 / 中等牌跟注或过牌 / 弱牌弃牌
- 兜底触发时写入本地日志（前端不展示，便于调试 LLM 质量）

#### 4.9.5 节奏与体验
- AI 行动前随机延迟 1–3 秒，模拟思考，避免机械感
- AI 淘汰、结算、柱状图与真人赛事完全一致
- API Key 未配置 / 测试连接失败时，单机入口置灰并给出引导文案

#### 4.9.6 用量与成本控制
- 每次决策仅 1 次 API 调用（解析失败最多重试 1 次，之后直接兜底）
- prompt 上下文裁剪：只含当前手信息，不携带历史手
- 预估成本：deepseek-chat，一场赛事约 50–150 次决策，单场 < ¥0.1

#### 4.9.7 引擎复用
- 游戏引擎为独立 TS 模块（`engine/`），与 UI 解耦
- **单机模式**：引擎跑在浏览器内，AI 决策经本地 Node 代理（`/api/ai/decide`）转发 DeepSeek（规避浏览器 CORS 与 Key 暴露问题）
- **联机模式**：引擎跑在 Host Node 进程内

---

## 5. 网络方案

### 5.1 整体架构

```
┌─────────────────┐         ┌─────────────────┐
│  Player A 浏览器 │         │  Player B 浏览器 │
│  (Client)       │         │  (Client)       │
└────────┬────────┘         └────────┬────────┘
         │ WebSocket                 │ WebSocket
         │ (TCP)                     │
         ▼                            ▼
   ┌─────────────────────────────────────────┐
   │          Host 进程 (Node.js)            │
   │  - 游戏状态机（权威）                    │
   │  - mDNS 服务广播                         │
   │  - WebSocket Server                      │
   └─────────────────────────────────────────┘
```

- **Host = 建赛者浏览器内启动的内嵌服务**（Electron-like 方案：浏览器内 Node Worker），或 Host = 房主本机起的 Node 进程
- **所有客户端为瘦客户端**：UI 渲染 + 事件上行；游戏状态由 Host 权威管理

> **单机 AI 模式例外**：引擎跑在浏览器内（无其他玩家需要同步），仅 AI 决策请求经本地 Node 代理转发 DeepSeek。

### 5.2 推荐方案：本地 Node.js + 浏览器前端 + mDNS

#### 5.2.1 技术栈
| 层 | 选型 | 说明 |
| --- | --- | --- |
| 前端 | React + Vite + TypeScript | 1440px 桌面优先 |
| 状态管理 | Zustand | 轻量 |
| 样式 | TailwindCSS + CSS Variables | 主题切换友好 |
| 实时通信 | Socket.IO | 自动重连 / 房间 / 事件 |
| mDNS | `bonjour-service`（Node） | 跨平台 mDNS 广播 |
| 后端 | Node.js (Express + Socket.IO) | 轻量、单进程 |
| 打包 | 前端 Vite build，后端 esbuild | 单二进制（可选 `pkg`） |
| AI | DeepSeek 官方 SDK（OpenAI 兼容） | 单机模式决策引擎 |

#### 5.2.2 端口规划
| 端口 | 用途 |
| --- | --- |
| 5173 | 前端 dev server（生产：静态托管） |
| 3000 | 后端 HTTP + WebSocket |
| 5353 (UDP) | mDNS 组播 |
| 3000 `/api/ai/decide` | AI 决策代理（转发 DeepSeek，仅本机使用） |

#### 5.2.3 启动流程

**房主侧**：
1. 打开 `http://localhost:5173`
2. 点击 "创建赛事"
3. 配置赛事参数
4. Host 进程启动：`node server.js --port 3000`
5. mDNS 广播 `_poque._tcp` 服务，实例名为 `赛事名-房间码`

**玩家侧**：
1. 打开同一网址 `http://<房主IP>:5173`
2. 首页自动列出 mDNS 发现的房间
3. 点击卡片或输入房间码加入

### 5.3 网络协议（WebSocket 消息）

#### 5.3.1 客户端 → 服务端
| 事件 | Payload | 说明 |
| --- | --- | --- |
| `create_room` | `{ name, mode, maxPlayers, initChips, blindStart }` | 建赛（mode: solo / lan） |
| `join_room` | `{ code, nickname, emoji }` | 加入 |
| `leave_room` | `{ }` | 离开 |
| `ready` | `{ }` | 标记就绪 |
| `start_game` | `{ }` | 房主开局 |
| `action` | `{ handId, type, amount? }` | 操作（fold/call/bet/raise） |
| `kick` | `{ playerId }` | 房主踢人 |

#### 5.3.2 服务端 → 客户端
| 事件 | Payload | 说明 |
| --- | --- | --- |
| `room_state` | `Room` | 全量房间状态 |
| `hand_start` | `{ handId, dealer, blinds, holeCards }` | 新一手开始 |
| `community` | `{ cards, stage }` | 公共牌翻出 |
| `action_required` | `{ playerId, timeLimit }` | 轮到某人 |
| `action_resolved` | `{ playerId, action }` | 操作结果 |
| `hand_result` | `{ winners, potDistribution }` | 一手结束 |
| `player_eliminated` | `{ playerId }` | 玩家被淘汰 |
| `tournament_end` | `{ finalRanking }` | 赛事结束 |
| `error` | `{ code, msg }` | 错误 |

### 5.4 同网段发现流程

1. **Host 调用 mDNS announce**：每秒 1 次广播，TTL 60s
2. **Client 启动 mDNS browse**：监听 `_poque._tcp.local`
3. **匹配**：Client 收到实例列表，更新 UI
4. **加入**：Client 通过 `room.code` 匹配，决定是否需要密码
5. **重连**：mDNS 超时但 WebSocket 仍在 → 保持连接；WebSocket 断开 → 尝试重连，超时后清除条目

### 5.5 备选方案

#### 5.5.1 零依赖方案（推荐给最小可用版本）
- **不依赖 Node 后端**：纯静态前端 + WebRTC + 信令服务器
- **信令服务器**：用一个免费的公共信令（如 PeerJS Cloud）做发现与握手
- **优点**：零部署，浏览器打开即用
- **缺点**：依赖第三方服务，可控性弱

#### 5.5.2 公网中转方案（不在本期范围）
- 部署一个小型中转服务器（Vercel + Supabase Realtime）
- 任何网络都能玩
- 后期可考虑，本期不做

### 5.6 网络方案选择汇总

| 方案 | 部署难度 | 跨网段 | 推荐度 |
| --- | --- | --- | --- |
| 本地 Node + mDNS（**本期主推**） | 中 | ❌ | ⭐⭐⭐⭐⭐ |
| 零依赖 WebRTC + 公共信令 | 低 | 部分 | ⭐⭐⭐ |
| 公网中转 | 中 | ✅ | ⭐⭐（下期） |

---

## 6. AI 对战方案（DeepSeek 单机模式）

### 6.1 架构

```
┌──────────────────────────────┐
│  浏览器（单机模式）           │
│  ├─ 游戏引擎（TS 模块）       │
│  └─ UI 渲染                  │
└──────────┬───────────────────┘
           │ POST /api/ai/decide
           ▼
┌──────────────────────────────┐
│  本地 Node 代理               │
│  （复用联机模式的 Host 进程） │
└──────────┬───────────────────┘
           │ HTTPS
           ▼
┌──────────────────────────────┐
│  DeepSeek API                │
│  deepseek-chat               │
└──────────────────────────────┘
```

### 6.2 API 接入
- 绑定 **DeepSeek 官方 SDK**（OpenAI 兼容协议，`baseURL: https://api.deepseek.com`）
- API Key 在设置页配置，存 localStorage，仅本机使用，不经任何远端服务器持久化
- 默认模型 `deepseek-chat`；可选 `deepseek-reasoner`（更慢但推理更强）

### 6.3 Prompt 设计（要点）
- **System Prompt**：NLHE 规则简述 + 输出格式约束（严格 JSON，禁止解释性文字）
- **User Prompt**：当前状态 JSON
  - 自己的手牌 + 公共牌
  - 底池总额、本轮各家已下注额、各家剩余筹码
  - 位置信息（庄家位 / 盲注位）
  - 本手行动历史（谁做了什么）
  - **合法动作列表**（含金额上下限）
- 期望输出：`{ "action": "fold | check | call | bet | raise | all_in", "amount": n }`

### 6.4 兜底与降级
| 异常场景 | 处理 |
| --- | --- |
| 返回非 JSON / 解析失败 | 重试 1 次，仍失败 → 规则引擎 |
| 动作非法（不在合法列表） | 重试 1 次（附错误提示），仍失败 → 规则引擎 |
| 超时 8 秒无响应 | 直接规则引擎 |
| API 报错（余额不足 / Key 失效） | 弹窗提示用户，本手由规则引擎接管，可在设置页修复后恢复 LLM |
| 网络完全不可用 | 整场赛事自动降级为纯规则 Bot，继续可玩 |

### 6.5 风格说明
- 不提供风格选项，AI 行为由 LLM 在规则约束内自主发挥
- 不注入人格 prompt，保持行为自然多样

### 6.6 未来扩展（本期不做）
- 改为 OpenAI 兼容通用配置（自定义 baseURL，接入 GLM / Kimi / Qwen 等）
- AI 人格化（每个 AI 独立风格 prompt：紧凶 / 松凶 / 跟注站）
- 联机模式 AI 补位（缺人时拉 AI 凑桌）

---

## 7. 数据模型

### 7.1 房间 Room
```typescript
interface Room {
  id: string;              // 内部 UUID
  code: string;            // 4 位数字（单机模式无）
  name: string;
  hostId: string;
  mode: 'solo' | 'lan';    // solo = 单机 AI 模式，lan = 局域网联机
  maxPlayers: number;      // 2–9
  initChips: number;
  blindStart: number;      // 起始级别 1–10
  blindMode: 'manual' | 'auto';
  theme: string;
  status: 'lobby' | 'playing' | 'finished';
  players: Player[];
  createdAt: number;
}
```

### 7.2 玩家 Player
```typescript
interface Player {
  id: string;
  nickname: string;
  emoji: string;
  isHost: boolean;
  isReady: boolean;
  isConnected: boolean;
  isAI: boolean;           // 单机模式中的 AI 玩家
  chips: number;
  seatIndex: number;       // 0–8
}
```

### 7.3 一手 Hand
```typescript
interface Hand {
  id: string;
  handNumber: number;      // 第几手
  dealerIndex: number;
  smallBlindIndex: number;
  bigBlindIndex: number;
  blindLevel: number;
  communityCards: Card[];
  pot: number;
  players: HandPlayer[];   // 本手参与玩家
  actions: Action[];
  stage: 'preflop' | 'flop' | 'turn' | 'river' | 'showdown' | 'ended';
}

interface HandPlayer {
  playerId: string;
  holeCards: Card[];       // 私有
  bet: number;             // 本手总投入
  folded: boolean;
  allIn: boolean;
  hasActed: boolean;
}
```

### 7.4 操作 Action
```typescript
type ActionType = 'fold' | 'check' | 'call' | 'bet' | 'raise' | 'all_in';
interface Action {
  playerId: string;
  type: ActionType;
  amount?: number;
  timestamp: number;
}
```

---

## 8. UI / 视觉规范

### 8.1 设计原则
- **极简**：去除一切不必要的装饰
- **留白**：牌桌周围留出充足空间
- **对比**：文字与背景对比度 ≥ 4.5:1
- **一致性**：圆角、阴影、间距遵循同一尺度

### 8.2 设计令牌（Design Tokens）
```css
:root {
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 20px;
  --radius-pill: 999px;

  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
  --shadow-md: 0 4px 12px rgba(0, 0, 0, 0.08);
  --shadow-lg: 0 12px 32px rgba(0, 0, 0, 0.12);

  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 40px;
}
```

### 8.3 牌面设计
- 尺寸：72×104px（桌面端）
- 圆角：12px
- 阴影：`shadow-md`
- 字体：Helvetica Bold / SF Pro
- 红桃/方块：红色 `#DC2626`
- 黑桃/梅花：黑色 `#1F1F1F`
- 牌背：主题色 + 主题纹理

### 8.4 参考截图复刻要点
- 椭圆牌桌（SVG ellipse，宽高比约 16:9）
- 玩家头像环绕排列（按座位索引计算极坐标）
- 操作条底部居中，三按钮水平排列
- 比例快捷键（25%/33%/75%/133%/ALL）在操作条上方
- 每个玩家位显示：头像 + 昵称 + 积分 + 当前下注
- 中央公共牌区 5 个等宽牌位

---

## 9. 非功能性需求

| 项目 | 要求 |
| --- | --- |
| 浏览器支持 | Chrome 120+, Edge 120+, Safari 17+, Firefox 120+ |
| 屏幕尺寸 | 最低 1440×900，推荐 1920×1080 |
| 性能 | 操作响应 ≤ 200ms（局域网）；AI 决策 ≤ 8s（含兜底） |
| 并发 | 单 host 支持 9 客户端，10 局/分钟吞吐 |
| 可靠性 | 服务端状态机无状态外依赖，断电恢复不要求 |
| 部署 | 单进程启动（Node），无需数据库 |

---

## 10. 开发路线图

### Phase 1 — 本地热座 MVP（1 周）
- 单机版，无网络
- 一局完整打到底
- 视觉规范与截图对齐

### Phase 2 — 局域网联机（1 周）
- Node + Socket.IO 后端
- 房间创建/加入/开始
- mDNS 自动发现

### Phase 3 — AI 单机模式（1 周）
- DeepSeek SDK 接入 + 设置页 Key 配置
- 决策管线（prompt 构造 / JSON 校验 / 重试）
- 规则兜底引擎
- 单机赛事流程复用（含结算柱状图）

### Phase 4 — 完整赛事流程（1 周）
- MTT 盲注递增
- 断线重连
- 结算柱状图
- 主题切换

### Phase 5 — 打磨与发布（3 天）
- 边缘 case
- 视觉打磨
- 文档与打包

> **总周期**：约 4.5 周到可玩版本

---

## 11. 风险与决策记录

| 风险 | 缓解 |
| --- | --- |
| mDNS 在某些家用路由器被禁用 | 备用方案：手动输入 host IP（保留接口） |
| 浏览器 Socket.IO 兼容性问题 | Socket.IO 4.x 已支持所有主流浏览器 |
| 玩家断线 5 分钟体验差 | 缩短为 2 分钟；超时就剩的玩家继续 |
| 桌面端限制失去潜在用户 | 本期目标明确，后期再做适配 |
| LLM 输出不稳定导致卡局 | 规则引擎兜底 + 8s 超时，赛事永不中断 |
| DeepSeek 费用超预期 | 每决策 1 次调用、上下文裁剪，单场 < ¥0.1 |

### 已确认决策（2026-09-11）
1. ✅ Web 应用（浏览器，桌面优先）
2. ✅ 局域网 mDNS 自动发现
3. ✅ 联机模式纯热座轮流，无 AI 补位（v0.2 起新增单机 AI 模式，见 21–25）
4. ✅ 2–9 人（满桌）
5. ✅ 标准 No-Limit Hold'em
6. ✅ MTT 式盲注递增（预设表）
7. ✅ 限时 15s 超时自动 Check/Fold
8. ✅ 断线保留席位，间隙自动 Check/Fold
9. ✅ 淘汰制结束（只剩 1 人）
10. ✅ 结算只显示排名柱状图
11. ✅ 不要氛围功能（聊天/表情/音效）
12. ✅ 每局"局数"= 淘汰赛，无固定手数限制
13. ✅ 默认 emoji / 字母头像
14. ✅ 主页面只显示加入按钮 + 房间码手动输入
15. ✅ 每人初始积分相同，建赛者统一设置
16. ✅ 建赛者权限：仅踢人 / 结束赛事
17. ✅ 主题：单色背景 + 亮色牌桌（v0.3 修订：精简为 Light / Dark 两套）
18. ✅ 仅桌面端（1440px 起）
19. ✅ 玩家身份：昵称 + emoji 头像
20. ✅ 4 位纯数字房间码

### 已确认决策 v0.2（2026-09-11 第二轮）
21. ✅ 产品更名为 **Poque**
22. ✅ 新增 AI 单机模式，绑定 **DeepSeek 官方 SDK**（deepseek-chat）
23. ✅ AI 数量可调（1–8 个），统一风格，不提供风格选项，LLM 自主发挥
24. ✅ LLM 决策失败/超时自动退回规则引擎兜底，网络不可用整场降级为规则 Bot
25. ✅ AI 模式复用完整赛事流程（命名 / 初始积分 / 盲注递增 / 淘汰制 / 结算柱状图）

---

## 12. 附录

### 12.1 术语表
| 术语 | 含义 |
| --- | --- |
| Hand | 一手牌，从翻前到摊牌 |
| Tournament / Session / 赛事 | 一整场游戏，从开局到结算 |
| NLHE | No-Limit Hold'em，无限注德州 |
| MTT | Multi-Table Tournament，多桌锦标赛（此处借指盲注递增模式） |
| Pot | 底池 |
| Chip | 积分（替代 token） |

### 12.2 后续待办（v0.3 待补充）
- [ ] 操作条滑块的具体行为
- [ ] 弃牌后玩家位的视觉变化细节
- [ ] 玩家头像预设池的具体清单（30 个）
- [ ] mDNS 实例名格式（命名规则）
- [ ] 房间码冲突时的提示文案
- [ ] 翻牌/转牌/河牌的过渡动画时长
- [ ] 端局全押摊牌时的展示顺序
- [ ] AI 决策 prompt 完整模板（System + User）
- [ ] 规则兜底引擎的参数细节（Chen 公式阈值 / 位置修正表）
- [ ] AI 昵称 emoji 预设池最终清单
- [ ] DeepSeek 用量统计展示（设置页显示本次赛事消耗 token 数）
